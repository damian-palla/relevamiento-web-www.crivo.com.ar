{
  "name": "cypress-crivo",
  "version": "1.0.0",
  "description": "Tests E2E con Cypress para crivo.com.ar",
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "cy:run:headed": "cypress run --headed",
    "report:clean": "rimraf cypress/reports",
    "report:merge": "mochawesome-merge cypress/reports/mochawesome/mochawesome*.json > cypress/reports/report.json",
    "report:generate": "marge cypress/reports/report.json -o cypress/reports/html --charts --reportFilename test-report --reportTitle \"E2E Test Report — crivo.com.ar\" --reportPageTitle \"E2E Test Report — crivo.com.ar\"",
    "test:report": "npm run report:clean && cypress run && npm run report:merge && npm run report:generate"
  },
  "devDependencies": {
    "cypress": "^13.17.0",
    "mochawesome": "^7.1.4",
    "mochawesome-merge": "^5.1.1",
    "mochawesome-report-generator": "^6.3.2",
    "rimraf": "^6.1.3"
  }
}
